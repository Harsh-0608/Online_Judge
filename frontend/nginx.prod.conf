server {
    listen 80;
    server_name 13.239.63.88.sslip.io;
    return 301 https://$host$request_uri; # Redirect HTTP traffic to HTTPS
}

server {
    listen 443 ssl;
    server_name 13.239.63.88.sslip.io;

    ssl_certificate /etc/letsencrypt/live/13.239.63.88.sslip.io/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/13.239.63.88.sslip.io/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://backend:5000/api;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
